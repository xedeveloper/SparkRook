/// [HttpMethods] are the set of default
/// methods are predefined with
/// * GET
/// * HEAD,
/// * POST,
/// * PUT,
/// * DELETE,
/// * CONNECT,
/// * OPTIONS,
/// * TRACE,
/// * PATCH,
enum HttpMethod {
  GET,
  HEAD,
  POST,
  PUT,
  DELETE,
  CONNECT,
  OPTIONS,
  TRACE,
  PATCH,
}
